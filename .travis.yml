language: python
sudo: true
python:
  - "2.6"
  - "2.7"
install:
  - sudo apt-get -y install mlocate  
  - projects/bootstrap.sh
  - sudo updatedb
  - python -c "import sys ; print sys.path"
  - export PYTHONPATH=$PYTHONPATH:/usr/lib/python2.7/dist-packages:/usr/lib/python2.6/dist-packages
  - python -c "import sys ; print sys.path"
script:
  - # locate manatee
  - sudo lsof -i
  - ps auxww > _ps
  - cat _ps
  - cat /opt/lindat/kontext/log/application.log
  - curl http://localhost:5000/
  - echo "will be moved to lindat-kontext..."